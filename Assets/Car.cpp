#include "Car.h"

void Car::Update(KeyboardClient & Kbd)
{
}

void Car::Draw(D3DGraphics & gfx)
{
    // Car car;
    // for( int width = 0; width < 40; ++width )
    // {
    //     for (int height = 0; height < 40; ++height)
    //     {
    //         gfx.DrawLine(int StartX, int StartY, int EndX, int EndY, D3DCOLOR Color);
    //     }
    // }
}
